# 安装与快速开始

交付版本：v1.2.1 候选更新。先在空白模型试用，不直接拿重要设计文件做首轮测试。

## 一、安装更新后的 Skill

解压后，真正的技能根目录是包含 `SKILL.md` 的 `sketchup-design-studio` 文件夹。

已有旧版本时先备份到 Skill 扫描目录之外，例如桌面的 `skill-backups`，避免两个目录中出现同名 Skill。保留你自己的本地改动，再把完整新文件夹复制到以下位置：

| 系统 | 完整目标目录 |
|---|---|
| Windows | `%USERPROFILE%\.agents\skills\sketchup-design-studio` |
| macOS | `~/.agents/skills/sketchup-design-studio` |

不要形成 `sketchup-design-studio/sketchup-design-studio/SKILL.md` 的双层嵌套。不要只复制 `SKILL.md`，新增运行时依赖 `scripts`、`examples` 和 `references` 的相对位置。更新后重新打开 Codex，并重启 SketchUp 后再加载运行时，避免新旧 Ruby 文件缓存混用。

本包不包含原压缩包的 `.git`。已有 Git 工作目录应保留原 `.git`，基于候选分支检查差异；远程发布状态以本次交付说明与对应 PR 为准。

## 二、先在 SketchUp 里试一次

需要目标为 2022 或更新的 SketchUp 桌面版。先新建空白模型；若模板含默认人物，在这个测试模型中清除它。不要打开重要项目。退出群组 / 组件编辑，把活动标签设为 Untagged / Layer0。

打开 SketchUp 的 Ruby 控制台，在不同版本界面中查找“Ruby Console / Ruby 控制台”。执行：

```ruby
load File.expand_path('~/.agents/skills/sketchup-design-studio/scripts/sketchup_studio.rb')
SketchupDesignStudio.demo
```

若用户目录解析与实际安装位置不同，换成实际完整路径，例如：

```ruby
load 'C:/Users/Z/.agents/skills/sketchup-design-studio/scripts/sketchup_studio.rb'
SketchupDesignStudio.demo
```

`Z` 只是路径示例，不要默认它就是 Windows 登录用户名。Mac 可同样使用 `/Users/实际用户名/...` 的完整路径。

**预期看到的效果**：地台先出现，四根柱子逐步长高，随后安装梁架、逐根铺设格栅顶棚，最后放置坐凳。中文面板同步显示阶段、步数和下一步。演示共 70 步，不是录像文件。

## 三、面板怎么用

“暂停”在当前步骤结束后暂停；“继续”恢复；“下一步”只在暂停时执行一个步骤。“停止生成”保留此前成功部分，不能恢复同一个已停止任务。0.5 / 1 / 2 / 4 倍速改变展示间隔；快速模式使用同一几何计划快速分批执行，仍不保证完全无卡顿。

关闭窗口会暂停。重新打开：

```ruby
SketchupDesignStudio.show_panel
```

状态检查：

```ruby
puts SketchupDesignStudio.status_brief_json
```

默认使用最近一次任务；自动化工作台必须保存并传入完整 `job_id`，以免拿错任务。导出记录只写 JSON，不保存模型；打开文件选择框前会暂停正在运行的任务。

生成时可以观察视图，但不要同时编辑、撤销、切换模型或进入组件。发生这些变化，保护逻辑可能停止任务，防止后续步骤继续使用过期对象。

## 四、让 Codex 给真实项目使用

将以下文字交给本机已能操作 SketchUp 的 Codex：

```text
用 $sketchup-design-studio 完成本轮建模，并展示真实逐步生成过程。
先读取 references/generation-animation.md，检查本机 SketchUp Ruby 执行通道。
加载 scripts/sketchup_studio.rb，按照 examples/task_template.rb 的 steps 契约生成任务，
提交 mode: :animated，保留固定 plan_id 和返回的 job_id。
不要直接执行一整段同步建模，不要按换行硬拆 Ruby，不要先建完再假装播放进度。
提交后结束当前 Ruby 调用，由工作台本地调度 status_brief_json；进度不要逐步触发 Astra 回合。
只在 completed、completion_valid 为真且 assert_completed!(job_id) 通过后，另存新版本并进行模型验收。
不删除原模型，不覆盖已有文件，不为动画重建整个旧项目。
```

这个包不包含你目前外部 Ruby 工作台的源码。因此，它增加的是可由工作台加载的执行器和接入契约，不会自动改变第三方工作台。若工作台默认把任何 Ruby 返回当作完成，需要按 `references/generation-animation.md` 接入后续状态查询。

## 五、常见问题

**找不到文件**：确认安装目录，使用完整路径和正斜线。路径里可有中文或空格，但要完整包在引号内。

**提示已有任务或相同计划**：先重开面板查看当前任务。停止不删除已有成果；同 `plan_id` 重复执行会被拒绝。不要为了消除提示清空正式模型。演示请另开空白模型。

**模型突然全部出现 / 没有逐步变化**：核对是否选择快速模式、是否仍在运行旧同步脚本、steps 是否只有一个大动作。必须把真正的几何创建拆成小步骤；多次更新文字不等于建模动画。

**暂停短暂不响应**：当前步骤太重时需等它完成，再将那一步拆细。该执行器不能中断单次 SketchUp 几何运算。

**修改期间任务失败**：不要自动重跑。先核对错误步骤和已完成部分，保留原结果，再决定剩余任务或新变体。模型观察器是辅助防护，不是对其他插件的绝对隔离。

**升级后行为不一致**：停止任务，保存需要保留的结果，重启 SketchUp 后重新加载运行时。Ruby 会缓存已加载文件，不建议运行中热更新。

## 六、这次还没有实测的部分

真实 SketchUp 2022+ 的几何效果、CEF 对本地面板及安全策略的加载、Windows / macOS 表现、第三方工作台的异步返回、`.skp` 保存重开与 Enscape 联动，需要本机逐项验证。源码测试已通过，不代表这些环节已经通过。

## 六、v1.2.1 完成状态

完成后撤销、重做或检测到新模型提交，旧完成证明失效。请重新核对现状，不改写属性绕过门禁。此接口不拦截 SketchUp 原生保存。详见 [验收清单](docs/实机验收清单.md)。
