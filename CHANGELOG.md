# 更新记录

## 1.2.1 · 2026-09-15 · 候选更新，原生验收待完成

- 修复完成后撤销仍可能被旧内存状态放行的问题；核对六个持久化检查点字段。
- 完成后保留轻量观察器，撤销、重做与检测到的后续提交使旧证明失效；保守同模型范围，不声称全几何检测。
- 新增 completion_valid、completion_reason 与 revision。面板不将执行历史等同当前可交付状态。
- 新增 status_brief / status_brief_json 与 detail: :brief；原全量接口默认行为保留，错误摘要截断，全文按需查。
- 最近任务最多 16 个，清出时释放监视器；UI 通知在本地事件循环合并，不在观察器里修改几何。
- 新增本地通知策略示例、回归测试、状态字节测量工具、路线图与实机验收清单；未自动接管外部工作台。
- v1.3 构件库/入口精简、v1.4 视觉与跨软件适配仅规划，未列为本版能力。
- 历史截图和报告不作为本版测试证据，本次结果见 docs。

## 1.2.0 · 2026-09-15

### 新增

- SketchUp 主线程单次计时器驱动的分步生成执行器；演示与快速模式共享步骤计划。
- 中文 HtmlDialog 面板：阶段进度、暂停 / 继续 / 单步 / 停止、速度与模式切换、JSON 记录。
- `submit`、`status_json`、`control`、`show_panel`、`assert_completed!` 等本地接入接口。
- 任务组、逻辑标识、重复计划防护、模型 / 编辑路径 / 活动标签检查及模型事件观察器。
- 70 步亭架演示、13 步墙体模板、可选盒体与顶面推拉辅助函数。
- Ruby 模拟测试、JavaScript 测试、结构 / 语法检查与后续 GitHub CI 配置。
- 安装说明、异步接入协议、测试报告及面板浏览器预览。

### 修改

- `SKILL.md` 增加新建任务生成过程规则，明确局部改稿不强制重建。
- Ruby 流程明确同步事务与动画每步事务的区别，纠正异步返回与完成的混淆风险。
- README、中文简介和默认调用提示同步到 1.2.0；历史验证与本次验证分开列示。
- 模式切换不重新生成，阶段统计预计算，历史与警告有上限，结束后释放步骤闭包与临时数据。

### 保留与限制

- 保留原有尺度控制、中文命名、构件编辑、材质、改稿、场景及 Enscape 流程。
- 不是外部工作台源码改造；不是任意旧 Ruby 的自动动画转换器。
- 停止保留已完成几何；每步有独立撤销记录。不提供视频导出、整次单步撤销、跨重启恢复或自动失败重试。
- 不自带 Ruby 沙箱，不自动保存模型、清库、读取凭证、开网络服务或覆盖已有文件。
- 本次未在真实 SketchUp 中运行；模型几何与宿主兼容性待实机验收。

## 1.1.1 · 原压缩包版本

原 Skill 与四份设计 / 执行相关文档的设计能力基础。门窗配合、节点检查及历史案例说明沿用原仓库，未把历史测试算入 1.2.0 的新增验证。
