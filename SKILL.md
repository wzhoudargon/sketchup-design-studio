---
name: sketchup-design-studio
description: 面向建筑与室内设计的 SketchUp 方案建模与迭代。用于依据图纸、草图、参考图或已有 SU 模型进行体量推敲、空间布局、立面开口、材料分区、局部改稿及方案比选，形成可继续编辑的模型；适用于“拉模型”“调整空间”“改门窗”“做 A/B 方案”等涉及 SketchUp 模型的任务。
---

# SketchUp 空间设计工作室

围绕设计意图组织模型：空间尺度有依据，构件关系可追踪，局部调整能够延续上一轮成果。向设计师说明体量、开间、进深、标高、界面与材料的变化，把软件实现留在执行层。

## 确定本轮设计工作面

从现有资料和对话提取本轮要解决的空间问题、设计阶段与交付用途。沿用已经明确的选择，只补问会改变方案方向或造成重大返工的信息。

| 工作阶段 | 模型表达重点 | 适宜的完成深度 |
|---|---|---|
| 概念推敲 | 体量组合、虚实关系、空间序列与主要尺度 | 用轻量几何表达关系，体量可独立调整；暂缓门窗构造、收口及精细配景 |
| 方案深化 | 平面组织、竖向标高、立面节奏、洞口及材料分区 | 建立有厚度的主要界面和明确的构件连接，保留局部替换与比选能力 |
| 交付整备 | 已确定方案的构件层级、材料映射、图面视角与文件可编辑性 | 按交接对象整理模型与必要依赖，验证接收方最常用的修改操作 |

阶段可以混合，例如主体保持体块表达、入口局部深化；用任务范围决定精度。缺少精度要求时先采用足以回答当前设计问题的最轻表达，说明假设后推进。

先确认实际可用的 SketchUp 版本、桌面或 Web 环境及操作通道。Skill 提供工作方法，不自带 SketchUp 执行环境。具备应用内操作能力时生成并验证 `.skp`；只能交付生成程序时，明确为待在兼容桌面版内执行的 `.rb`，交付方式无法满足用户用途时先说明限制。

## 建立尺度与定位基准

将资料分为尺寸依据、现状约束和意向参考。标注图纸或实测数据控制几何，意向图控制形态与材料倾向；发生冲突时点明差异，不用风格参考覆盖已知边界。

- **尺度校准**：图像平面以可靠标注校准，区分轴线尺寸、结构尺寸和完成面净尺寸；有第二项独立尺寸时交叉核对。扫描变形与透视畸变须单独判断，单一比例尺不能证明全图准确。
- **空间定位**：交代基准轴、方向和零点标高；沿用已有模型的单位及坐标约定。新建模型按场景选用毫米或米并说明，既有坐标需转换时保留变换关系。
- **信息等级**：明确尺寸、由几何关系推导的尺寸和暂估尺寸分别说明。透视图可支持比例推敲，不能直接提供未标注的精确尺寸；无可靠尺度时可先做标明假设的比例模型。
- **底图核对**：将参考资料置于独立且锁定的对象中。先检查主要轮廓、轴网和标高，再细化依赖这些基准的构件。只在关键尺寸或设计方向存在歧义时等待澄清；已授权的完整建模无需逐层重复确认。

## 以空间和构造关系组织模型

模型大纲应能回答“这是什么、位于哪里、哪些部分会一起变化”。通常由单体、楼层或空间分区进入构件；小型室内或单件设计可直接采用区域与对象两级。

**划分编辑单元。** 按后续调整范围组织体量、围护界面、楼板、屋面、开口与装配构件。方案阶段的墙体可按实际墙段划分，并保持转角和端部合理连接；无需为命名拆碎连续几何，也不要把内外墙皮误建成两套重叠实体。构造层或饰面仅在厚度表达、独立调整或交付用途需要时分离。

**落实构件拆分。** 方案深化中，将门窗总成内需要分别调整的框、扇、玻璃组织为可辨认的子对象；窗台、遮阳、五金等按本轮设计深度增设。框料截面、开启扇及玻璃配色应有各自的修改入口。同一连续构件仅需表面分色时直接分面赋材质，不为配色把每个面拆成群组；概念体块阶段可保留简化开口。

**管理构件族。** 同规格且预期同步变化的对象共享组件定义，差异规格采用独立定义；修改单个实例前检查共享范围，必要时唯一化。以基准角点、轴线或底面设置插入基点。规格变化优先修改尺寸或定义，避免非等比缩放扭曲型材截面与贴图。

**区分归属与显示。** 大纲承载空间归属，标签承载构件类别或方案显隐。原始边面及活动标签保持 `Untagged/Layer0`，分类赋给群组或组件。检查父子标签组合，避免隐藏门窗时连带遮掉墙体。命名遵循下文的统一规则，保持同类对象可检索、同一对象跨轮次可追踪。

**保持几何可操作。** 需要推拉的界面保留平整、闭合的轮廓；实体检查厚度、外向正面、转角、交叠及洞口贯通。薄片、曲面与地形按其用途判断，不强求全部封闭。重复配景优先实例化，曲面分段与贴图分辨率随设计精度调整。

既有模型先识别有用的层级和依赖，修复本轮范围内的问题。导入网格无法可靠还原构造关系时，标明保留网格与重建部分；不要用全局炸开、自动清库代替判断。

## 统一设计命名

名称采用中文设计词汇，以 ` · ` 分隔字段，固定同类对象的字段顺序。项目内先统一楼层、方位、空间与材料词表，例如楼层统一写“二层”，同一立面不交替写“南侧”和“南立面”。

| 对象 | 字段顺序 | 示例 |
|---|---|---|
| 群组与组件实例 | 类型 · 空间或界面 · 楼层 · 序号 | `窗 · 南立面 · 二层 · 01` |
| 组件定义 | 类型 · 构造或形式 · 规格 | `窗 · 双扇平开 · 1200×1500mm` |
| 材料 | 材料类别 · 作用分区 · 表面特征 · 序号 | `矿物涂层 · 外立面 · 暖灰 · 01` |
| 标签 | 分类维度 · 类别 | `构件 · 门窗`、`方案 · A`、`资料 · 底图` |
| 场景 | 方案 · 观察部位 · 表达方式 | `方案A · 南立面 · 透视` |

同一同类命名范围内序号从 `01` 起，至少两位且不重复；改名或移位后保留稳定对象标识，新增对象接续编号，不在每次改稿时全量重排。组件定义的规格字段明确单位与尺寸含义，同一类型统一约定宽×高或宽×深×高。复用同一材料时复用名称与材料对象，需要独立变化时才分配新分区或序号。

单层或单件任务可省略不适用字段，但同一类对象统一省略；无比选时场景省略方案字段。`Untagged/Layer0` 等系统名称保持原样。新建模型完整采用本规则；既有项目对本轮整理范围建立旧名与新名对应，更新相关引用，无关对象保留，后续同范围改稿沿用该对应关系。

## 将改稿落实到关联构件

把自然语言修改转为“目标对象、定位基准、变化量、关联范围”，优先利用选中对象、名称、截图标注和已有设计记录定位。只有定位或设计含义不清时补问。

修改前识别会随动的对象与应保留的内容。例如扩大开间，要确定哪一侧固定、哪一侧移动；不能直接等比放大房间内所有构件。

- 调整门窗尺寸或位置时，核对宿主墙洞、窗台、过梁及邻接饰面；本阶段尚未建模的构造不因此自动深化。几何独立并不意味着具有自动联动能力，按当前工具实际能力更新并复核。
- 变更标高、墙线或板边时，检查与楼板、屋面、开口和固定装配构件的衔接。家具是否随空间边界移动按布置意图决定，保留明确指定的手工摆位。
- 在原有模型上实施能可靠定位的局部更新，避免每次改稿都重建全案。程序生成的关键对象采用稳定标识；纯手工模型不要求补建完整参数系统。
- 遇到无法分辨的手改内容或共享关系冲突，保留现有对象，在独立版本中处理受影响部分，并指出需要设计师决定的差异。全案复制、覆盖原件或重置材料不得作为默认解法。

每轮反馈聚焦设计变化：哪些尺寸、构件或材料已更新，哪些内容保留，哪些关联仍待处理。任务跨轮次较多时，将基准版本、关键假设和对象对应关系简要随模型保存，便于接续改稿。

## 材料分区与方案比选

按设计意图建立材料分区：同色但需要独立推敲的内外界面使用不同材料对象，同用途且应统一调整的构件复用材料。材料名称按统一设计命名组织，例如 `矿物涂层 · 外立面 · 暖灰 · 01`、`金属 · 窗框 · 深灰哑光 · 01`。

通常在表面明确赋材质，保持上级容器默认；采用实例级配色时，让需要继承的内部表面保持默认并说明作用范围。框料与玻璃独立控制，核对透明度、面朝向、纹理方向和实际铺贴尺度。双面可见的薄片按用途处理背面，不能用背面涂色掩盖实体翻面。检查材料时关闭按标签着色。

**保留材料备选。** 既有材料即使暂未赋给可见表面，也可能属于待选方案，默认保留。自动清理仅限本轮新建、确认无引用且无备选用途的冗余材料；检查包含隐藏对象、备选组与组件定义。颜色相同不构成合并依据，已有材料的删除或合并只在用户明确要求的范围内执行。

仅在需要比选时建立 A/B 表达。可采用局部备选组或独立文件，明确哪些构件继续共享、哪些定义与材料已分离；切换方案后验证显隐和配色没有串联。

对比视图采用相同机位、投影与光照条件，突出本轮设计变量。SketchUp 场景用于保存视图、剖切和可见性等状态，不负责保存不同的几何尺寸或材料版本；真实差异由备选对象或文件承载。未经用户要求，不把每次局部修改扩展成多方案制作或渲染任务。

## 素材来源与依赖

使用外部构件、第三方模型或贴图时，随交付维护简短来源清单，记录对应对象或材料名、素材名称、原始来源链接或提供方、已知作者与许可说明，以及实际使用的文件或包内相对路径。用户提供但来源不明的资料标为“用户提供，来源待补”，缺失作者或授权信息如实标明，不臆造授权结论；链接不携带账号凭证或临时访问令牌。

只列本轮实际使用的素材及其依赖；替换素材时更新对应关系。模型中已嵌入的贴图也保留来源记录，外链资源核对交接后的可访问性；需要随包分发时按已知使用条件处理，避免将不可分发的整套素材库打入交付包。

## 形成便于接手的设计交付

交付与当前阶段一致。根据用途准备总览、平面、关键立面或剖切场景，单件模型不必套用整套建筑视图；能实际操作模型时给出必要预览。保留原文件，以清晰的版本名保存成果，核对接收方使用环境和必要的贴图、外链依赖。

验收以真实操作为依据：检查关键尺度与定位，抽查构件可独立编辑、共享组件联动符合预期，材料调整不会误影响其他分区；改过的洞口和连接部位单独复核。有条件时保存并重开文件，抽查修改后恢复检查状态。按“已验证、待验证、不适用”报告，脚本语法通过不等于模型通过。

附简短的设计交接说明：本轮设计变化、尺度假设、待定事项，以及继续调整空间边界、单个构件和材料分区的具体入口。最终只把实际生成且验证状态明确的文件称为成果；未运行 SketchUp 时，明确交付脚本及加载方法，不将图片、OBJ 或更改扩展名的文件称为 `.skp`。

## 程序化建模

需要编写或运行 SketchUp Ruby 程序时，读取 [Ruby 执行与改稿约定](references/ruby-workflow.md)。以少量可理解的设计参数驱动当前任务，只有反复调整确有收益时才增加参数面板或复用工具，不预设通用插件开发。
