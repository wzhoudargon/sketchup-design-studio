# v1.2.1｜SketchUp 与实际工作台验收清单

状态：**原生验收未执行。** 模拟测试不替代本清单；不要在唯一正式模型上测试。

环境：日期____；操作系统____；SketchUp 完整版本____；工作台/桥接版本____；运行时版本____；提交 SHA____；测试模型副本____。

| 项目 | 操作与应有结果 | 结果/证据 |
|---|---|---|
| 加载 | 完整替换 Skill，重启 SU；加载不自动建模、不保存 | 待测 |
| 空白演示 | 70 步：地台、四柱、梁架、顶棚格栅、坐凳 | 待测 |
| 控制 | 暂停不继续变形；单步一次推进一步；继续到完成 | 待测 |
| 模式 | 快速与演示最终尺寸、构件一致，不叠建 | 待测 |
| 关窗 | 生成时关闭面板会暂停，重开可继续 | 待测 |
| 停止 | 保留成功部分，不自动删除或重试 | 待测 |
| 完成门禁 | 运行/暂停/停止/失败不放行；完整且未变更才返回模型 | 待测 |
| 原生撤销 | 完成后撤销最后一步：completion_valid=false，门禁拒绝 | 待测 |
| 原生重做 | 撤销后重做：不自动恢复旧证明，需重新验收 | 待测 |
| 后续编辑 | 完成后修改同模型对象，旧证明保守失效，不自动修复 | 待测 |
| 仅浏览 | 环绕/缩放不失效；进编辑暂拒，退出且未编辑可恢复 | 待测 |
| 模型切换 | 不向新模型继续写，旧任务不作为新模型交付依据 | 待测 |
| 重复提交 | 同 plan_id 拒绝，不随机换 ID 或删旧组重试 | 待测 |
| 保存重开 | 验收后另存不存在的新文件，重开核对几何/属性/材质 | 待测 |
| 真实几何 | 关键尺寸、法线、生长、构件位置、可编辑性符合意图 | 待测 |
| 工作台 | 轮询留本地，正常进度不触发逐步模型回合，终态去重 | 待测 |
| 资源 | 多任务、关模型、重开面板无持续重复通知/明显监视器积累 | 待测 |

控制台检查（使用 submit 返回的完整 job_id）：

```ruby
SketchupDesignStudio::VERSION
puts SketchupDesignStudio.status_brief_json(job_id)
model = SketchupDesignStudio.assert_completed!(job_id)
```

门禁不是原生保存钩子，用户或其他扩展直接保存不会被它拦截。不能改写任务属性或内存状态绕过失效提示。失败时记录操作顺序、截图、完整 status_json 和具体错误，保留模型副本；不要原样重跑整案。

Gate A：通过 / 不通过 / 部分通过。未通过项与限制____。正式发布决定____。
